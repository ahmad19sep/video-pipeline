# Build Specification

The authoritative build specification is `MASTER_DOCUMENTATION.md`. This repository follows its roadmap in strict phase order.

Phase 0 acceptance criteria are maintained in `docs/implementation-plan.md`. Later phases must not begin until the active phase checks pass.
