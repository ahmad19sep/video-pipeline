style:
  name: fast
  visual_change_target_seconds: 7.0
  transition_density: none
  caption_preset: clean-two-line
  effect_budgets:
    transitions_per_minute: 0
    camera_moves_per_minute: 2
    fullscreen_broll_ratio: 0.25
    animated_text_per_minute: 4
    impact_sfx_per_minute: 2
transcription:
  preset: fast
network:
  enabled: false
